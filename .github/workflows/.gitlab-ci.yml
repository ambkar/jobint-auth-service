stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - docker build -t auth-image .

test-job:
  stage: test
  script:
    - docker run --rm auth-image pytest -s -v

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
